#!/usr/bin/env node
const app = require('../app');

try {
    const port = process.env.PORT || 3000;
    app.listen(port);
    console.log(`Microsservice uploader listening at http://localhost:${port}`);
} catch (e) {
    throw e
}